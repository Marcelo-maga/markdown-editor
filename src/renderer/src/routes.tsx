import React from 'react'
import { BrowserRouter, Switch } from 'react-router-dom'

const Routes: React.FC = () => {
  return (
    <BrowserRouter>
      <Switch>

      </Switch>
    </BrowserRouter>
  )
}

export { Routes }
